{
  "name": "mld-dictionary-ts",
  "version": "SNAPSHOT",
  "repository": "github:moonlitdoor/mld-dictionary-ts",
  "license": "MIT",
  "scripts": {
    "postinstall": "cd dictionary-ts && npm install",
    "lint": "cd dictionary-ts && npm run lint",
    "build": "cd dictionary-ts && npm run build",
    "test": "cd dictionary-ts && npm run test"
  }
}
